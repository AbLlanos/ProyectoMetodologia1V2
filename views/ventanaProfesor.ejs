<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <script src="https://cdn.tailwindcss.com"></script>
    <title>Profesor</title>
</head>

<body>


    <!-- Barra de navegación -->
    <section class="w-full px-8 text-gray-700 bg-black mb-0">
        <div class="container flex flex-col flex-wrap items-center justify-between py-5 mx-auto md:flex-row max-w-7xl">
            <div class="relative flex flex-col md:flex-row">
                <a href="index.html"
                    class="flex items-center mb-5 font-medium text-gray-900 lg:w-auto lg:items-center lg:justify-center md:mb-0">
                    <img src="img/0001-logoNavbar.webp" alt="logoNav" width="150px" height="100px">
                    <span class="mx-auto text-xl font-black leading-none text-white select-none"><span
                            class="text-indigo-600"></span></span>
                </a>
            </div>
            <div class="inline-flex items-center ml-5 space-x-6 lg:justify-end">
                <a href="inicioSesion.html"
                    class="text-base font-medium leading-6 text-white whitespace-no-wrap transition duration-150 ease-in-out hover:text-gray-400">
                    Iniciar sesión
                </a>
                <a href="registroUsuario.html"
                    class="inline-flex items-center justify-center px-4 py-2 text-base font-medium leading-6 text-white whitespace-no-wrap bg-indigo-600 border border-transparent rounded-md shadow-sm hover:bg-indigo-500 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-600">
                    Registrarse
                </a>
            </div>
        </div>
    </section>

    <!-- Tabla de profesor-->
    <section id=" container tablaEstudiante" class="bg-gray-100 p-6 rounded-lg shadow-lg">
        <h1 class="text-center text-2xl font-bold text-gray-800">ITSQMET</h1>
        <h2 class="text-center text-xl font-semibold text-gray-700 mt-2">Unidades de vinculación</h2>
        <div class="h-1 bg-teal-500 mx-auto mt-3 rounded-full w-32"></div>
        <p class="text-center text-gray-600 mt-3">
            Tus credenciales y opciones de gestión se mostrarán a continuación:
        </p>

        <!-- Tabla para mostrar los datos -->
        <div class="overflow-x-auto mt-6">
            <table class="min-w-full bg-white border border-gray-200 rounded-lg table">
                <thead>
                    <tr>
                        <th
                            class="px-6 py-3 text-left font-bold uppercase tracking-wider border-b border-gray-300 bg-blue-500">
                            Campo
                        </th>
                        <th
                            class="px-6 py-3 text-left font-bold uppercase tracking-wider border-b border-gray-300 bg-blue-600">
                            Credenciales
                        </th>
                    </tr>
                </thead>
                <tbody>
                    <tr class="border-b">
                        <td class="px-6 py-3 font-semibold text-gray-700">ID</td>
                        <td class="px-6 py-3 text-gray-600">
                            <%= datos.idBase %>
                        </td>
                    </tr>
                    <tr class="border-b">
                        <td class="px-6 py-3 font-semibold text-gray-700">Periodo académico</td>
                        <td class="px-6 py-3 text-gray-600">
                            <%= datos.periodoAcademico %>
                        </td>
                    </tr>
                    <tr class="border-b">
                        <td class="px-6 py-3 font-semibold text-gray-700">Nombre</td>
                        <td class="px-6 py-3 text-gray-600">
                            <%= datos.nombreBase %>
                        </td>
                    </tr>
                    <tr class="border-b">
                        <td class="px-6 py-3 font-semibold text-gray-700">Apellido</td>
                        <td class="px-6 py-3 text-gray-600">
                            <%= datos.apellidoBase %>
                        </td>
                    </tr>
                    <tr class="border-b">
                        <td class="px-6 py-3 font-semibold text-gray-700">Cédula</td>
                        <td class="px-6 py-3 text-gray-600">
                            <%= datos.cedulaBase %>
                        </td>
                    </tr>
                    <tr class="border-b">
                        <td class="px-6 py-3 font-semibold text-gray-700">Teléfono</td>
                        <td class="px-6 py-3 text-gray-600">
                            <%= datos.telefonoBase %>
                        </td>
                    </tr>
                    <tr>
                        <td class="px-6 py-3 font-semibold text-gray-700">Rol</td>
                        <td class="px-6 py-3 text-gray-600">
                            <%= datos.rolBase %>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </section>





    <!-- Tabla de prácticas -->
    <section id="tablaPracticas" class="bg-gray-100 p-6 rounded-lg shadow-lg">
        <h1 class="text-center text-2xl font-bold text-gray-800">Prácticas Preprofesionales</h1>
        <div class="h-1 bg-teal-500 mx-auto mt-3 rounded-full w-32"></div>
        <p class="text-center text-gray-600 mt-3">
            A continuación se detallan las prácticas preprofesionales asignadas:
        </p>
        <section>
            <div class="text-center mt-6">
                <button id="cargarPracticasBtn"
                    class="px-4 py-2 text-white bg-blue-600 rounded hover:bg-blue-500 focus:outline-none">
                    Cargar Prácticas Preprofesionales
                </button>
            </div>

            <!-- Contenedor con desbordamiento horizontal -->
            <div class="overflow-x-auto mt-6 p-5">
                <table class="min-w-full text-sm text-left text-gray-500 border border-gray-300 rounded-lg">
                    <thead class="text-xs text-gray-700 uppercase bg-gray-200 dark:bg-gray-700 dark:text-gray-400">
                        <tr>
                            <th scope="col" class="px-6 py-3">ID Práctica</th>
                            <th scope="col" class="px-6 py-3">Nombre Estudiante</th>
                            <th scope="col" class="px-6 py-3">Cédula Estudiante</th>
                            <th scope="col" class="px-6 py-3">Entidad Beneficiaria</th>
                            <th scope="col" class="px-6 py-3">Fecha Inicio</th>
                            <th scope="col" class="px-6 py-3">Ubicación Inicio</th>
                            <th scope="col" class="px-6 py-3">Fecha Fin</th>
                            <th scope="col" class="px-6 py-3">Ubicación final</th>
                            <th scope="col" class="px-6 py-3">Horas Totales</th>
                            <th scope="col" class="px-6 py-3">Calificación</th>
                            <th scope="col" class="px-6 py-3">Estado</th>
                            <th scope="col" class="px-6 py-3"></th>
                            <th scope="col" class="px-6 py-3"></th>
                        </tr>
                    </thead>
                    <tbody id="tbody-carro" class="bg-white divide-y divide-gray-200">
                        <!-- Aquí se llenan dinámicamente los datos -->
                    </tbody>
                </table>
            </div>
        </section>


        <script>
            document.getElementById("cargarPracticasBtn").addEventListener("click", function () {
                const idDocente = "<%= datos.idBase %>"; // Obtener el ID del docente desde el backend

                // Verifica que el ID del docente es correcto
                console.log("Docente ID: ", idDocente);

                // Función para formatear las fechas
                function formatDate(dateString) {
                    const options = { year: 'numeric', month: '2-digit', day: '2-digit', hour: '2-digit', minute: '2-digit' };
                    const date = new Date(dateString);
                    return date.toLocaleString('es-ES', options);  // Formato: DD/MM/YYYY HH:mm
                }

                // Realiza una solicitud para obtener las prácticas preprofesionales
                fetch(`/practicasPreprofesionales/${idDocente}`)
                    .then(response => response.json())
                    .then(data => {
                        const tbody = document.getElementById("tbody-carro");
                        tbody.innerHTML = ''; // Limpiar las filas actuales de la tabla

                        // Verificar si no hay prácticas
                        if (data.length === 0) {
                            tbody.innerHTML = '<tr><td colspan="10" class="text-center text-gray-500 py-3">No se encontraron prácticas preprofesionales.</td></tr>';
                        } else {
                            // Si hay prácticas, crear las filas en la tabla
                            data.forEach(practica => {
                                const row = document.createElement("tr");

                                row.innerHTML = `
                        <td class="px-6 py-3">${practica.id_registro}</td>
                        <td class="px-6 py-3">${practica.usuario_estudiante}</td>
                        <td class="px-6 py-3">${practica.cedula_estudiante}</td>
                        <td class="px-6 py-3">${practica.entidad_beneficiaria}</td>
                        <td class="px-6 py-3">${formatDate(practica.hora_inicio)}</td>
                        <td class="px-6 py-3">${practica.ubicacion_inicio}</td>
                        <td class="px-6 py-3">${formatDate(practica.hora_final)}</td>
                        <td class="px-6 py-3">${practica.ubicacion_final}</td>
                        <td class="px-6 py-3">${practica.hora_total_visita}</td>
                        <td class="px-6 py-3">${practica.calificacion}</td>
                        <td class="px-6 py-3">${practica.estado}</td>
                        <td class="px-6 py-3">
                            <form action="/eliminarPractica" method="POST" style="display:inline;">
                                <input type="hidden" name="idPractica" value="${practica.id_registro}">
                                <button type="submit" class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded">Eliminar</button>
                            </form>
                        </td>
                        <td class="px-6 py-3">
                            <button class="bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded ml-2 editar-btn" data-id="${practica.id_registro}">Editar</button>
                        </td>
                    `;
                                tbody.appendChild(row);
                            });

                            // Agregar evento de edición
                            document.querySelectorAll('.editar-btn').forEach(button => {
                                button.addEventListener('click', function () {
                                    const idRegistro = this.getAttribute('data-id');
                                    // Solicitar datos del registro
                                    fetch(`/getPractica/${idRegistro}`)
                                        .then(response => response.json())
                                        .then(data => {
                                            function formatDateForInput(dateString) {
                                                const date = new Date(dateString);
                                                const year = date.getFullYear();
                                                const month = String(date.getMonth() + 1).padStart(2, '0'); // Mes con 2 dígitos
                                                const day = String(date.getDate()).padStart(2, '0'); // Día con 2 dígitos
                                                const hours = String(date.getHours()).padStart(2, '0'); // Hora con 2 dígitos
                                                const minutes = String(date.getMinutes()).padStart(2, '0'); // Minutos con 2 dígitos
                                                return `${year}-${month}-${day}T${hours}:${minutes}`;
                                            }

                                            // Cargar datos en el formulario
                                            document.getElementById('idUnidadVinculacion').value = data.id_unidadvinculacion;
                                            document.getElementById('nombreUnidadVinculacion').value = data.nombre_unidadvinculacion;
                                            document.getElementById('usuarioEstudiante').value = data.usuario_estudiante;
                                            document.getElementById('cedulaEstudiante').value = data.cedula_estudiante;
                                            document.getElementById('entidadBeneficiaria').value = data.entidad_beneficiaria;

                                            // Formatear y cargar las fechas
                                            document.getElementById('horaInicio').value = formatDateForInput(data.hora_inicio);
                                            document.getElementById('ubicacionInicio').value = data.ubicacion_inicio;
                                            document.getElementById('horaFin').value = formatDateForInput(data.hora_final);
                                            document.getElementById('ubicacionFinal').value = data.ubicacion_final;
                                            document.getElementById('calificacion').value = data.calificacion;
                                            document.getElementById('estado').value = data.estado;

                                            // Cambiar el texto del botón para "Actualizar"
                                            document.getElementById('actualizarBtn').innerText = 'Actualizar práctica';
                                            document.getElementById('visitaForm').action = `/actualizarPractica/${idRegistro}`;
                                        })
                                        .catch(error => console.error("Error al cargar la práctica:", error));
                                });
                            });
                        }
                    })
                    .catch(error => {
                        console.error("Error al cargar las prácticas:", error);
                        alert("Hubo un error al cargar las prácticas preprofesionales.");
                    });
            });

        </script>
    </section>




    <!-- FORMULARIO PARA CREAR PRACTICAS PREPROFESIONALES -->
    <section class="container mx-auto py-2 px-4 p-5" id="crearcuenta">
        <div class="flex flex-col items-center justify-center">
            <div class="bg-gray-800 p-8 rounded-lg shadow-lg w-full max-w-4xl">
                <div class="flex justify-center mb-6">
                    <img width="120px" height="120px" src="img/0001-logoNavbar.webp" alt="Logo de la Empresa">
                </div>
                <h1 class="text-center text-white text-2xl mb-4">Generar la práctica pre-profesional</h1>
                <hr>
                <h2 class="text-center text-white text-2xl mb-4 pt-3">Llene los siguientes espacios</h2>

                <form id="visitaForm" action="/crearRegistroPractica" method="post">
                    <!-- Campo Unidad ID -->
                    <div class="flex flex-col">
                        <label for="idUnidadVinculacion" class="block text-white mb-2">ID de la unidad de
                            vinculación</label>
                        <input type="text" name="idUnidadVinculacion" id="idUnidadVinculacion"
                            class="w-full p-3 mb-4 rounded-md border border-gray-300"
                            placeholder="ID de la unidad de vinculación" required readonly value="<%= datos.idBase %>">
                    </div>

                    <!-- Campo Unidad nombre -->
                    <div class="flex flex-col">
                        <label for="nombreUnidadVinculacion" class="block text-white mb-2">Nombre de la unidad de
                            vinculación</label>
                        <input type="text" name="nombreUnidadVinculacion" id="nombreUnidadVinculacion"
                            class="w-full p-3 mb-4 rounded-md border border-gray-300" placeholder="Nombre del docente"
                            required readonly value="<%= datos.nombreBase %> <%= datos.apellidoBase %>">
                    </div>

                    <!-- Campo Estudiante Usuario-->
                    <div class="flex flex-col">
                        <label for="usuarioEstudiante" class="block text-white mb-2">Ingresar el usuario del
                            estudiante</label>
                        <input type="text" name="usuarioEstudiante" id="usuarioEstudiante"
                            class="w-full p-3 mb-4 rounded-md border border-gray-300"
                            placeholder="Usuario del estudiante" required>
                    </div>

                    <!-- Campo cedula estudiante -->
                    <div class="flex flex-col">
                        <label for="cedulaEstudiante" class="block text-white mb-2">Ingresar la cédula del
                            estudiante</label>
                        <input type="text" name="cedulaEstudiante" id="cedulaEstudiante"
                            class="w-full p-3 mb-4 rounded-md border border-gray-300"
                            placeholder="Cédula del estudiante" required maxlength="10">
                    </div>

                    <!-- Otros campos de información -->
                    <div class="flex flex-col">
                        <label for="entidadBeneficiaria" class="block text-white mb-2">Escribir el nombre de la entidad
                            beneficiaria</label>
                        <input type="text" name="entidadBeneficiaria" id="entidadBeneficiaria"
                            class="w-full p-3 mb-4 rounded-md border border-gray-300" required
                            placeholder="Entidad beneficiaria">
                    </div>

                    <div class="flex flex-col">
                        <label for="horaInicio" class="block text-white mb-2">Fecha y hora de inicio</label>
                        <input type="datetime-local" name="horaInicio" id="horaInicio"
                            class="w-full p-3 mb-4 rounded-md border border-gray-300" required>
                    </div>

                    <div class="flex flex-col">
                        <label for="ubicacionInicio" class="block text-white mb-2">Ubicación de inicio</label>
                        <input type="text" name="ubicacionInicio" id="ubicacionInicio"
                            class="w-full p-3 mb-4 rounded-md border border-gray-300" required
                            placeholder="Ubicación de inicio">
                    </div>

                    <div class="flex flex-col">
                        <label for="horaFin" class="block text-white mb-2">Fecha y hora de fin</label>
                        <input type="datetime-local" name="horaFin" id="horaFin"
                            class="w-full p-3 mb-4 rounded-md border border-gray-300" required>
                    </div>

                    <div class="flex flex-col">
                        <label for="ubicacionFinal" class="block text-white mb-2">Ubicación final</label>
                        <input type="text" name="ubicacionFinal" id="ubicacionFinal"
                            class="w-full p-3 mb-4 rounded-md border border-gray-300" required
                            placeholder="Ubicación final">
                    </div>

                    <div class="flex flex-col">
                        <label for="calificacion" class="block text-white mb-2">Calificación</label>
                        <input type="number" name="calificacion" id="calificacion" min="0" max="10" step="0.1"
                            class="w-full p-3 mb-4 rounded-md border border-gray-300"
                            placeholder="Calificación de la práctica" required>
                    </div>

                    <div class="flex flex-col">
                        <label for="estado" class="block text-white mb-2">Estado de la práctica</label>
                        <select name="estado" id="estado" class="w-full p-3 mb-4 rounded-md border border-gray-300"
                            required>
                            <option value="En progreso">En progreso</option>
                            <option value="Finalizada">Finalizada</option>
                            <option value="Cancelada">Cancelada</option>
                        </select>
                    </div>

                    <!-- Campo para capturar o subir una foto -->
                    <div class="flex flex-col">
                        <label for="fotoPractica" class="block text-white mb-2">Foto de la práctica</label>
                        <input type="file" name="fotoPractica" id="fotoPractica"
                            class="w-full p-3 mb-4 rounded-md border border-gray-300 bg-white" required>
                    </div>

                    <!-- Botón de Enviar -->
                    <button type="submit"
                        class="w-full py-3 mt-4 bg-blue-500 text-white font-bold rounded-md hover:bg-blue-600"
                        id="actualizarBtn">Generar o actualizar práctica</button>
                </form>
            </div>
        </div>
    </section>



</body>

</html>