<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <script src="https://cdn.tailwindcss.com"></script>
    <title>Administrador</title>
</head>

<body>


    <!-- Barra de navegación -->
    <section class="w-full px-8 text-gray-700 bg-black mb-0">
        <div class="container flex flex-col flex-wrap items-center justify-between py-5 mx-auto md:flex-row max-w-7xl">
            <div class="relative flex flex-col md:flex-row">
                <a href="index.html"
                    class="flex items-center mb-5 font-medium text-gray-900 lg:w-auto lg:items-center lg:justify-center md:mb-0">
                    <img src="img/0001-logoNavbar.webp" alt="logoNav" width="150px" height="100px">
                    <span class="mx-auto text-xl font-black leading-none text-white select-none"><span
                            class="text-indigo-600"></span></span>
                </a>
            </div>
            <div class="inline-flex items-center ml-5 space-x-6 lg:justify-end">
                <a href="inicioSesion.html"
                    class="text-base font-medium leading-6 text-white whitespace-no-wrap transition duration-150 ease-in-out hover:text-gray-400">
                    Iniciar sesión
                </a>
                <a href="registroUsuario.html"
                    class="inline-flex items-center justify-center px-4 py-2 text-base font-medium leading-6 text-white whitespace-no-wrap bg-indigo-600 border border-transparent rounded-md shadow-sm hover:bg-indigo-500 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-600">
                    Registrarse
                </a>
            </div>
        </div>
    </section>

    <!-- Tabla de admin-->
    <section id=" container tablaEstudiante" class="bg-gray-100 p-6 rounded-lg shadow-lg">
        <h1 class="text-center text-2xl font-bold text-gray-800">ITSQMET</h1>
        <h2 class="text-center text-xl font-semibold text-gray-700 mt-2">Administrador</h2>
        <div class="h-1 bg-teal-500 mx-auto mt-3 rounded-full w-32"></div>
        <p class="text-center text-gray-600 mt-3">
            Tus credenciales y opciones de gestión se mostrarán a continuación:
        </p>

        <!-- Tabla para mostrar los datos -->
        <div class="overflow-x-auto mt-6">
            <table class="min-w-full bg-white border border-gray-200 rounded-lg table">
                <thead>
                    <tr>
                        <th
                            class="px-6 py-3 text-left font-bold uppercase tracking-wider border-b border-gray-300 bg-blue-500">
                            Campo
                        </th>
                        <th
                            class="px-6 py-3 text-left font-bold uppercase tracking-wider border-b border-gray-300 bg-blue-600">
                            Credenciales
                        </th>
                    </tr>
                </thead>
                <tbody>
                    <tr class="border-b">
                        <td class="px-6 py-3 font-semibold text-gray-700">ID</td>
                        <td class="px-6 py-3 text-gray-600">
                            <%= datos.idBase %>
                        </td>
                    </tr>
                    <tr class="border-b">
                        <td class="px-6 py-3 font-semibold text-gray-700">Periodo académico</td>
                        <td class="px-6 py-3 text-gray-600">
                            <%= datos.periodoAcademico %>
                        </td>
                    </tr>
                    <tr class="border-b">
                        <td class="px-6 py-3 font-semibold text-gray-700">Nombre</td>
                        <td class="px-6 py-3 text-gray-600">
                            <%= datos.nombreBase %>
                        </td>
                    </tr>
                    <tr class="border-b">
                        <td class="px-6 py-3 font-semibold text-gray-700">Apellido</td>
                        <td class="px-6 py-3 text-gray-600">
                            <%= datos.apellidoBase %>
                        </td>
                    </tr>
                    <tr class="border-b">
                        <td class="px-6 py-3 font-semibold text-gray-700">Cédula</td>
                        <td class="px-6 py-3 text-gray-600">
                            <%= datos.cedulaBase %>
                        </td>
                    </tr>
                    <tr class="border-b">
                        <td class="px-6 py-3 font-semibold text-gray-700">Teléfono</td>
                        <td class="px-6 py-3 text-gray-600">
                            <%= datos.telefonoBase %>
                        </td>
                    </tr>
                    <tr>
                        <td class="px-6 py-3 font-semibold text-gray-700">Rol</td>
                        <td class="px-6 py-3 text-gray-600">
                            <%= datos.rolBase %>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </section>





    <!-- Tabla de modificacion -->
    <section id="tablaUsuarios" class="bg-gray-100 p-6 rounded-lg shadow-lg">
        <h1 class="text-center text-2xl font-bold text-gray-800">Usuarios Registrados</h1>
        <div class="h-1 bg-teal-500 mx-auto mt-3 rounded-full w-32"></div>
        <p class="text-center text-gray-600 mt-3">
            A continuación se detallan los usuarios registrados:
        </p>
        <section>

            <!-- Contenedor con desbordamiento horizontal -->
            <div class="overflow-x-auto mt-6 p-5">
                <table class="min-w-full text-sm text-left text-gray-500 border border-gray-300 rounded-lg">
                    <thead class="text-xs text-gray-700 uppercase bg-gray-200 dark:bg-gray-700 dark:text-gray-400">
                        <tr>
                            <th scope="col" class="px-6 py-3">Nombre</th>
                            <th scope="col" class="px-6 py-3">Apellido</th>
                            <th scope="col" class="px-6 py-3">Cédula</th>
                            <th scope="col" class="px-6 py-3">Correo Electrónico</th>
                            <th scope="col" class="px-6 py-3">Rol</th>
                            <th scope="col" class="px-6 py-3"></th>
                            <th scope="col" class="px-6 py-3"></th>
                        </tr>
                    </thead>
                    <tbody id="tbody-usuarios" class="bg-white divide-y divide-gray-200">
                        <!-- Aquí se llenan dinámicamente los datos -->
                    </tbody>
                </table>
            </div>
        </section>
    </section>



    <script>
        // Realiza una solicitud para obtener los usuarios
        fetch('/usuarios')
            .then(response => response.json())
            .then(data => {
                const tbody = document.getElementById("tbody-usuarios");
                tbody.innerHTML = ''; // Limpiar las filas actuales de la tabla

                // Verificar si no hay usuarios
                if (data.length === 0) {
                    tbody.innerHTML = '<tr><td colspan="6" class="text-center">No hay usuarios registrados</td></tr>';
                } else {
                    // Llenar la tabla con los usuarios
                    data.forEach(usuario => {
                        const tr = document.createElement('tr');
                        tr.innerHTML = `
                            <td>${usuario.nombre}</td>
                            <td>${usuario.apellido}</td>
                            <td>${usuario.cedula}</td>
                            <td>${usuario.correo_electronico}</td>
                            <td>${usuario.rol}</td>
<td class="px-6 py-3">
    <form action="/eliminarUsuario" method="POST" style="display:inline;">
        <input type="hidden" name="idUsuario" value="${usuario.id_usuario}">
        <button type="submit" class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded">
            Eliminar
        </button>
    </form>
</td>

                            <td>
                                <button class="bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded ml-2 editar-btn" data-id="${usuario.id_usuario}">Editar</button>
                            </td>
                        `;
                        tbody.appendChild(tr);
                    });

                    // Agregar event listener a los botones de editar
                    const editButtons = document.querySelectorAll('.editar-btn');
                    editButtons.forEach(button => {
                        button.addEventListener('click', (event) => {
                            const userId = event.target.getAttribute('data-id');

                            // Solicitar los datos del usuario para editar
                            fetch(`/editarUsuario/${userId}`)
                                .then(response => response.json())
                                .then(usuario => {
                                    // Llenar el formulario con los datos del usuario
                                    document.getElementById('nombre').value = usuario.nombre;
                                    document.getElementById('apellido').value = usuario.apellido;
                                    document.getElementById('cedula').value = usuario.cedula;
                                    document.getElementById('correo').value = usuario.correo_electronico;
                                    document.getElementById('rol').value = usuario.rol;

                                    // Actualizar la acción del formulario para editar
                                    const form = document.getElementById('modificarUsuarioForm');
                                    form.action = `/actualizarUsuario/${usuario.id_usuario}`; // Asignar la URL correcta para el envío
                                })
                                .catch(error => console.error('Error al obtener los datos del usuario:', error));
                        });
                    });


                }
            })
            .catch(error => console.error('Error al obtener los usuarios:', error));
    </script>




    <!-- Formulario para modificar usuario -->
    <!-- Formulario para modificar usuario -->
    <section class="container mx-auto py-2 px-4 text-1xl" id="crearcuenta">
        <div class="flex flex-col items-center justify-center">
            <div class="bg-gray-800 p-8 rounded-lg shadow-lg w-full max-w-4xl">
                <div class="flex justify-center mb-6">
                    <img width="120px" height="120px" src="img/0001-logoNavbar.webp" alt="Logo de la Empresa">
                </div>
                <h1 class="text-center text-white text-2xl mb-4">Modificar cuenta</h1>
                <hr>
                <h2 class="text-center text-white text-2xl mb-4 pt-3">Llene los siguientes espacios</h2>
                <!-- Formulario -->
                <form id="modificarUsuarioForm" action="" method="POST">
                    <!-- Campo Nombre -->
                    <label for="nombre" class="block text-white mb-2">Ingrese su nombre:</label>
                    <input type="text" name="nombre" id="nombre"
                        class="w-full p-3 mb-4 rounded-md border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500"
                        required>

                    <!-- Campo Apellido -->
                    <label for="apellido" class="block text-white mb-2">Ingrese su apellido:</label>
                    <input type="text" name="apellido" id="apellido"
                        class="w-full p-3 mb-4 rounded-md border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500"
                        required>

                    <!-- Campo Cédula -->
                    <label for="cedula" class="block text-white mb-2">Ingrese su cédula:</label>
                    <input type="text" name="cedula" id="cedula"
                        class="w-full p-3 mb-4 rounded-md border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500"
                        required maxlength="10">

                    <!-- Campo Correo -->
                    <label for="correo" class="block text-white mb-2">Ingrese su correo electrónico:</label>
                    <input type="email" name="correo" id="correo"
                        class="w-full p-3 mb-4 rounded-md border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500"
                        required>

                    <!-- Campo Rol -->
                    <label for="rol" class="block text-white mb-2">Seleccione su rol:</label>
                    <select name="rol" id="rol"
                        class="w-full p-3 mb-4 rounded-md border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500">
                        <option value="estudiante">Estudiante</option>
                        <option value="vinculacion practicas">Vinculación Prácticas</option>
                        <option value="administrador">Administrador</option>
                    </select>

                    <!-- Botón de Enviar -->
                    <button type="submit"
                        class="w-full py-3 mt-4 bg-blue-500 text-white font-bold rounded-md hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500">Actualizar</button>
                </form>
            </div>
        </div>
    </section>




</body>

</html>